import styled from "styled-components";

import colors from "../../../assets/colors";

const WarningStyle = styled.h1`
  color: ${colors.red};
  font-size: 18px;
  text-decoration: none;
  text-align: center;
`;

export default WarningStyle;
